version: "3.3"
services:
  web:
    image: node:14
    command: sh -c "yarn install --immutable --production=false && yarn start"
    user: node
    stdin_open: true
    working_dir: /src
    ports:
      - 3000:3000
    volumes:
      - $PWD/:/src
